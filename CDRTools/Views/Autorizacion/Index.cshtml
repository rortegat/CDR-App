@model IEnumerable<CDRTools.Models.Autorizacion>

<h2>Códigos de Autorización</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Autorizacion_Codigo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Extensione.Extension_Descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Autorizacion_Codigo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Extensione.Extension_Descripcion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_Autorizacion }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_Autorizacion }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_Autorizacion })
        </td>
    </tr>
}

</table>
